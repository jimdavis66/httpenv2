services:
  httpenv2:
    build:
      context: .
    container_name: httpenv2
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=development
    # volumes:
    #   - .:/usr/src/app
    #   - /usr/src/app/node_modules
    restart: unless-stopped
